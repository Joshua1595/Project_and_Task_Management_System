# Production Port for Backend Server (e.g. Render)
PORT=3000

# Backend API URL for Frontend build (e.g. Vercel)
# Set this on Vercel to point to your backend deployed on Render, e.g. https://my-backend.onrender.com/api
VITE_API_BASE=/api
